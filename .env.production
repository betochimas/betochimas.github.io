# API base URL baked into the production build (`npm run build` / `npm run deploy`).
# TODO (D4): replace with the real hosted API host once it's deployed to Render,
# e.g. https://api.dylanchimasanchez.com
VITE_API_BASE_URL=https://api.dylanchimasanchez.com
