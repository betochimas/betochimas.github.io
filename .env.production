# API base URL baked into the production build (`npm run build` / Cloudflare Pages build).
# Points at the live Historical Conflicts API on Cloud Run (deployed 2026-05-27).
# Later: swap to https://api.dylanchimasanchez.com once that subdomain is mapped to the
# Cloud Run service (and add it to the API's APP_CORS_ALLOWED_ORIGINS).
VITE_API_BASE_URL=https://historical-conflicts-api-e7yywu4vqa-uw.a.run.app

# satorl analysis API (FastAPI on Cloud Run, deployed 2026-06-25). Used by /satorl.
VITE_SATORL_API_BASE_URL=https://satorl-api-1085902958075.us-west1.run.app
